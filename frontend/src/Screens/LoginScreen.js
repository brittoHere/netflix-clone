import React from "react";
import { Login } from "../Components";

const LoginScreen = () => {
  return (
    <div style={{ color: "#fff" }}>
      <Login />
    </div>
  );
};

export default LoginScreen;
